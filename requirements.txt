# Core (paper / NeurIPS release)
torch>=2.0
torch-geometric>=2.4
torch-scatter
torch-sparse
cyipopt>=1.3
numpy>=1.24
scipy>=1.10
pandas>=2.0
pyyaml>=6.0
tqdm>=4.65
pandapower>=2.13.0
openml>=0.14
mlcroissant>=1.0

# Optional dev / plotting (figures, sweeps)
matplotlib>=3.7.0
seaborn>=0.12.0
mlflow>=2.8.0
pytest>=7.4.0

# Hugging Face Hub (dataset mirror / cards — see huggingface/README.md)
huggingface_hub>=0.20.0
