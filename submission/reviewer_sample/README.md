# Reviewer sample (optional)

Three dual-label `.pt` files (two train + one test) for quick inspection without downloading full splits (~66 MB raw / ~27 MB tgz).

Tensor keys: `x`, `lam_g`, `zl`, `zu`, `mu`, `obj` (see code release `data/README_DATA.md`).
