from setuptools import setup, find_packages

setup(
    name="warp",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.10",
    description="WARP: Warm-start via Adversarial Residual Prior for AC-OPF",
)
