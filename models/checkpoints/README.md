# Model checkpoints

Place release checkpoints here for supplementary material:

| File | Description |
|------|-------------|
| `warp_gnores_case118.pt` | Best public WARP / EPD checkpoint (rename from internal training output). |
| `lstm_case118.pt` | IPM-LSTM-style baseline weights. |

These files are intentionally **not** committed when large; attach them to the OpenReview supplementary zip or use Git LFS for a public mirror.

HF Hub (optional): upload under [`dhruvsuri17`](https://huggingface.co/dhruvsuri17) per `huggingface/README.md`.
